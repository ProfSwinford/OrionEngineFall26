# Placeholder. Week 2 fills this in with the doctest target.
# It exists now so that ENGINE_BUILD_TESTS=ON does not break the Week 1 build.
add_custom_target(tests_placeholder
    COMMAND ${CMAKE_COMMAND} -E echo "No tests yet - see Week 2.")
